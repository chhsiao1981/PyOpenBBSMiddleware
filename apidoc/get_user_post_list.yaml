GetUserPostList
---
tags:
  - user
  - post
description: get ustr post list, sorted by time (from newest to oldest.)
parameters:
  - name: 'username'
    in: 'body'
    required: true
    description: "sub-string of the board-name (bid) to search for."
    schema:
      '$id': https://json-schema.org/draft/2019-09/output/schema
      type: string
  - name: 'newestTime'
    in: 'body'
    required: false
    description: "the newest-time in the list. -1 will starting from newest post if not specified. required if not the 1st-time."
    schema:
      '$id': https://json-schema.org/draft/2019-09/output/schema
      type: number
      default: -1
  - name: 'startID'
    in: 'body'
    required: false
    description: "starting postid, '' if not specified. required if not the 1st-time"
    schema:
      '$id': https://json-schema.org/draft/2019-09/output/schema
      type: string
      default: ''
  - name: 'max'
    in: 'body'
    required: false
    description: "max number of the returned posts, require <= 1000"
    schema:
      '$id': https://json-schema.org/draft/2019-09/output/schema
      type: number
      default: 1000
responses:
  200:
    description:
    schema:
      '$id': https://json-schema.org/draft/2019-09/output/schema
      type: object
      properties:
        list:
          type: array
          items:
            type: object
            properties:
              pid:
                type: string
                description: post-id
              postTime:
                type: number
                description: post-time in unix-time
              updateTime:
                type: number
                description: update-time in unix-time
              title:
                type: string
              bid:
                type: string
                description: board-id (brdname)
              read:
                type: boolean
                description: already-read or not.
              flag:
                type: number
              cat:
                type: string
                description: category
              nReader:
                type: number
              nRecommend:
                type: number
        next:
          type: string
          description: next pid. '' if reached end-of-the-list.
        nextTime:
          type: number
          description: next searching-time. 0 if reached end-of-the-list.
